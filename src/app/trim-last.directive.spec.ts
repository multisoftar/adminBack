import { TrimLastDirective } from './trim-last.directive';

describe('TrimLastDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimLastDirective();
    expect(directive).toBeTruthy();
  });
});
