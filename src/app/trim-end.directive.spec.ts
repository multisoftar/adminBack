import { TrimEndDirective } from './trim-end.directive';

describe('TrimEndDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimEndDirective();
    expect(directive).toBeTruthy();
  });
});
